import { X, Mail, Phone, Building2, Globe, Send } from 'lucide-react';
import { Supplier, STATUS_META } from '@/data';
interface Props { supplier: Supplier | null; onClose: () => void; onWrite: (id: string) => void; onOpenChat: (id: string) => void; onToggleBlacklist: (id: string) => void }
export function SupplierPanel({ supplier, onClose, onWrite }: Props) { if (!supplier) return null; const meta = STATUS_META[supplier.status]; return <><div className="fixed inset-0 z-40 bg-ink-900/10" onClick={onClose} /><aside className="fixed right-0 top-0 z-50 flex h-full w-[420px] max-w-[90vw] flex-col bg-white p-5 shadow-panel"><div className="flex justify-between"><div><h2 className="font-semibold text-ink-900">{supplier.name}</h2><span className={`mt-2 inline-block rounded-full px-2 py-1 text-2xs ${meta.badge}`}>{meta.label}</span></div><button onClick={onClose}><X className="h-4 w-4" /></button></div><div className="mt-6 space-y-4 text-sm text-ink-700"><p><Mail className="mr-2 inline h-4 w-4" />{supplier.email ?? 'Нет email'}</p><p><Phone className="mr-2 inline h-4 w-4" />{supplier.phone ?? 'Нет телефона'}</p><p><Building2 className="mr-2 inline h-4 w-4" />ИНН {supplier.inn}</p><p><Globe className="mr-2 inline h-4 w-4" />{supplier.site}</p><div><b>Позиции</b><p className="mt-2 text-xs">{supplier.items.join(' · ')}</p></div></div><div className="mt-auto"><button disabled={!supplier.email} onClick={() => onWrite(supplier.id)} className="inline-flex w-full items-center justify-center gap-2 rounded bg-accent-600 px-3 py-2 text-xs font-semibold text-white disabled:opacity-50"><Send className="h-3.5 w-3.5" />Написать</button></div></aside></>; }
